# Test Commands & Execution Reference

This document provides exact, verified commands for building, running, and testing all components of the system.

---

## 1. Backend Compilation

From the workspace root (`SkillGapAnalyzer/`):

### Windows (PowerShell)
```powershell
$files = (Get-ChildItem -Path 